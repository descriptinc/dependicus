## Reopened by Dependicus

{{#if isGroup}}
The **{{name}}** dependency group is non-compliant again. Reopening this issue instead of creating a new one.
{{else}}
**{{name}}** is non-compliant again (`{{currentVersion}}` installed, `{{latestVersion}}` available). Reopening this issue instead of creating a new one.
{{/if}}

{{#unless isFyi}}
- **Update type:** {{updateType}}
- **SLA window:** {{thresholdDays}} days
{{#if daysOverdue}}
- **Days overdue:** {{daysOverdue}}
{{/if}}
{{/unless}}
{{#each commentSections}}

### {{this.title}}

{{this.body}}
{{/each}}
