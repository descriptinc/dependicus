## Closed by Dependicus

{{#if isGroup}}
All dependencies in the **{{name}}** group are now compliant. Closing automatically.
{{else}}
{{#if currentVersion}}
**{{name}}** is now at version `{{currentVersion}}` (latest: `{{latestVersion}}`). Closing automatically.
{{else}}
**{{name}}** is no longer tracked by the current policy. Closing automatically.
{{/if}}
{{/if}}
