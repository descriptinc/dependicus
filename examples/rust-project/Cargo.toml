[package]
name = "dependicus-rust-example"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.210", features = ["derive"] }
tokio = { version = "1.40", features = ["full"] }

[dev-dependencies]
criterion = "0.5"
