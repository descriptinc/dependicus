## New versions available for {{packageName}}

Since this ticket was last updated, {{versionCountText}} been released:

{{#each versions}}
- **{{this.version}}** ({{formatDateShort this.publishDate}}){{#if this.formattedSize}} — {{this.formattedSize}}{{/if}}
  - [npm]({{this.npmUrl}}){{#if this.releaseUrl}} | [GitHub Release]({{this.releaseUrl}}){{/if}}
{{/each}}